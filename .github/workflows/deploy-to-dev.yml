on: 
  push: 
    branches: 
     - deploy_test
name: deployment-to-Dev
jobs:
  deploy:
    name: deploy to cluster
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@master

     
    - name: deploy to cluster
      uses: kodermax/kubectl-aws-eks@master
      env:
        kube_confg_data: ${{ secrets.KUBE_CONFIG_DATA_DEV }}
    - name: check kubectl version
      run: |
        kubectl config get-context
   
    #- name: deploy to Dev    
    #  run: |
    #    docker run -i -v /home/runner/work/DIGIT-DevOps/DIGIT-DevOps/deploy-as-code/helm:/opt/egov/helm egovio/egov-deployer:3-master-931c51ff ./egov-deployer deploy --helm-dir /opt/egov/helm -c=false -e dev egov-mdms-service:develop-6ada9d7702-2
      
