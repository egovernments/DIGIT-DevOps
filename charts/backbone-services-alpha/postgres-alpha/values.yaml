# Common Labels
name: "postgres-alpha-service"
labels:
  app: "postgres-alpha-service"
  group: "rainmaker"

# Ingress Configs
ingress:
  enabled: true
  zuul: true
  context: "postgres-alpha-service"

# Init Containers Configs
initContainers:
  dbMigration:
    enabled: true
    schemaTable: "postgres-alpha_service_schema"
    image:
      repository: "postgres-alpha-service-db"

# postgres-alpha-service/values.yaml
containerPort: 8080



# Container Configs
image:
  repository: "postgres-alpha-service"
  tag: "latest"
replicas: "1"
healthChecks:
  enabled: true
  livenessProbePath: "/postgres-alpha-service/health"
  readinessProbePath: "/postgres-alpha-service/health"
appType: "java-spring"
tracing-enabled: true
heap: "-Xmx64m -Xms64m"
java-args: "-Dspring.profiles.active=monitoring"

# postgres-alpha-service/values.yaml
service:
  type: ClusterIP
  port: 80

livenessProbe:
  initialDelaySeconds: 30
  periodSeconds: 10
  timeoutSeconds: 5
  successThreshold: 1
  failureThreshold: 3
  httpGet:
    path: /postgres-alpha-service/health
    port: 8080

readinessProbe:
  initialDelaySeconds: 30
  periodSeconds: 10
  timeoutSeconds: 5
  successThreshold: 1
  failureThreshold: 3
  httpGet:
    path: /postgres-alpha-service/health
    port: 8080

# Additional Container Envs
env:
  - name: SPRING_KAFKA_CONSUMER_GROUP_ID
    value: "postgres-alpha-service"
  - name: SPRING_KAFKA_PRODUCER_KEY_SERIALIZER
    value: "org.apache.kafka.common.serialization.StringSerializer"
  - name: SPRING_KAFKA_PRODUCER_VALUE_SERIALIZER
    value: "org.springframework.kafka.support.serializer.JsonSerializer"
  - name: PROCESS_postgres-alpha_LOGS_KAFKA_TOPIC
    value: "process-postgres-alpha-records"
  - name: PERSIST_postgres-alpha_LOGS_KAFKA_TOPIC
    value: "persist-postgres-alpha-records"
  # - name: JAVA_OPTS
  #   value: "{{ .Values.heap | quote }}"
  # - name: SERVER_PORT
  #   value: "8080"
  # - name: JAVA_ARGS
  #   value: "{{ .Values.java-args | quote }}"
  # - name: SECURITY_BASIC_ENABLED
  #   value: "false"  
  # - name: MANAGEMENT_SECURITY_ENABLED
  #   value: "false"
  # - name: EGOV_ENC_SIGN_HOST
  #   valueFrom:
  #     configMapKeyRef:
  #       name: "egov-service-host"
  #       key: "egov-enc-service"

