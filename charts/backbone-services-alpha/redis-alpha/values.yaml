# Common Labels
name: "redis-alpha-service"
labels:
  app: "redis-alpha-service"
  group: "rainmaker"

# Ingress Configs
ingress:
  enabled: true
  zuul: true
  context: "redis-alpha-service"

# Init Containers Configs
initContainers:
  dbMigration:
    enabled: true
    schemaTable: "redis-alpha_service_schema"
    image:
      repository: "redis-alpha-service-db"

# redis-alpha-service/values.yaml
containerPort: 8080



# Container Configs
image:
  repository: "redis-alpha-service"
  tag: "latest"
replicas: "1"
healthChecks:
  enabled: true
  livenessProbePath: "/redis-alpha-service/health"
  readinessProbePath: "/redis-alpha-service/health"
appType: "java-spring"
tracing-enabled: true
heap: "-Xmx64m -Xms64m"
java-args: "-Dspring.profiles.active=monitoring"

# redis-alpha-service/values.yaml
service:
  type: ClusterIP
  port: 80

livenessProbe:
  initialDelaySeconds: 30
  periodSeconds: 10
  timeoutSeconds: 5
  successThreshold: 1
  failureThreshold: 3
  httpGet:
    path: /redis-alpha-service/health
    port: 8080

readinessProbe:
  initialDelaySeconds: 30
  periodSeconds: 10
  timeoutSeconds: 5
  successThreshold: 1
  failureThreshold: 3
  httpGet:
    path: /redis-alpha-service/health
    port: 8080

# Additional Container Envs
env:
  - name: SPRING_KAFKA_CONSUMER_GROUP_ID
    value: "redis-alpha-service"
  - name: SPRING_KAFKA_PRODUCER_KEY_SERIALIZER
    value: "org.apache.kafka.common.serialization.StringSerializer"
  - name: SPRING_KAFKA_PRODUCER_VALUE_SERIALIZER
    value: "org.springframework.kafka.support.serializer.JsonSerializer"
  - name: PROCESS_redis-alpha_LOGS_KAFKA_TOPIC
    value: "process-redis-alpha-records"
  - name: PERSIST_redis-alpha_LOGS_KAFKA_TOPIC
    value: "persist-redis-alpha-records"
  # - name: JAVA_OPTS
  #   value: "{{ .Values.heap | quote }}"
  # - name: SERVER_PORT
  #   value: "8080"
  # - name: JAVA_ARGS
  #   value: "{{ .Values.java-args | quote }}"
  # - name: SECURITY_BASIC_ENABLED
  #   value: "false"  
  # - name: MANAGEMENT_SECURITY_ENABLED
  #   value: "false"
  # - name: EGOV_ENC_SIGN_HOST
  #   valueFrom:
  #     configMapKeyRef:
  #       name: "egov-service-host"
  #       key: "egov-enc-service"

