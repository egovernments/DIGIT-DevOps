releases:
  - name: cert-manager
    namespace: helmfile # Replace with your desired namespace
    chart: ./cert-manager
    version: 0.5.2 # Replace with the version of your chart, if needed
    values:
      - ./cert-manager/values.yaml
      
  - name: ingress-nginx
    namespace: helmfile # Replace with your desired namespace
    chart: ./ingress-nginx
    version: 1.0.0 # Replace with the version of your chart, if needed
    values:
      - ./ingress-nginx/values.yaml

  - name: minio
    namespace: helmfile # Replace with your desired namespace
    chart: ./minio
    version: 0.1.0 # Replace with the version of your chart, if needed
    values:
      - ./minio/values.yaml

  # - name: postgresql
  #   namespace: helmfile # Replace with your desired namespace
  #   chart: ./postgresql
  #   version: 11.9.13 # Replace with the version of your chart, if needed
  #   values:
  #     - ./postgresql/values.yaml

  # - name: redis
  #   namespace: helmfile # Replace with your desired namespace
  #   chart: ./redis
  #   version: 3.0.0 # Replace with the version of your chart, if needed
  #   values:
  #     - ./redis/values.yaml 

  # - name: elasticsearch
  #   namespace: helmfile # Replace with your desired namespace
  #   chart: ./elasticsearch
  #   version: 7.6.1 # Replace with the version of your chart, if needed
  #   values:
  #     - ./elasticsearch/values.yaml

  - name: kibana
    namespace: helmfile # Replace with your desired namespace
    chart: ./kibana
    version: 1.0.0 # Replace with the version of your chart, if needed
    values:
      - ./kibana/values.yaml

  # - name: kafka-kraft
  #   namespace: helmfile # Replace with your desired namespace
  #   chart: ./kafka-kraft
  #   version: 1.0.0 # Replace with the version of your chart, if needed
  #   values:
  #     - ./kafka-kraft/values.yaml                         