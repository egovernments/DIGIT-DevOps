# Common Labels
name: "postgres-service"
labels:
  app: "postgres-service"
  group: "rainmaker"

# Ingress Configs
ingress:
  enabled: true
  zuul: true
  context: "postgres-service"

# Init Containers Configs
initContainers:
  dbMigration:
    enabled: true
    schemaTable: "postgres_service_schema"
    image:
      repository: "postgres-service-db"

# postgres-service/values.yaml
containerPort: 8080



# Container Configs
image:
  repository: "postgres-service"
  tag: "latest"
replicas: "1"
healthChecks:
  enabled: true
  livenessProbePath: "/postgres-service/health"
  readinessProbePath: "/postgres-service/health"
appType: "java-spring"
tracing-enabled: true
heap: "-Xmx64m -Xms64m"
java-args: "-Dspring.profiles.active=monitoring"

# postgres-service/values.yaml
service:
  type: ClusterIP
  port: 80

livenessProbe:
  initialDelaySeconds: 30
  periodSeconds: 10
  timeoutSeconds: 5
  successThreshold: 1
  failureThreshold: 3
  httpGet:
    path: /postgres-service/health
    port: 8080

readinessProbe:
  initialDelaySeconds: 30
  periodSeconds: 10
  timeoutSeconds: 5
  successThreshold: 1
  failureThreshold: 3
  httpGet:
    path: /postgres-service/health
    port: 8080

# Additional Container Envs
env:
  - name: SPRING_KAFKA_CONSUMER_GROUP_ID
    value: "postgres-service"
  - name: SPRING_KAFKA_PRODUCER_KEY_SERIALIZER
    value: "org.apache.kafka.common.serialization.StringSerializer"
  - name: SPRING_KAFKA_PRODUCER_VALUE_SERIALIZER
    value: "org.springframework.kafka.support.serializer.JsonSerializer"
  - name: PROCESS_postgres_LOGS_KAFKA_TOPIC
    value: "process-postgres-records"
  - name: PERSIST_postgres_LOGS_KAFKA_TOPIC
    value: "persist-postgres-records"
  # - name: JAVA_OPTS
  #   value: "{{ .Values.heap | quote }}"
  # - name: SERVER_PORT
  #   value: "8080"
  # - name: JAVA_ARGS
  #   value: "{{ .Values.java-args | quote }}"
  # - name: SECURITY_BASIC_ENABLED
  #   value: "false"  
  # - name: MANAGEMENT_SECURITY_ENABLED
  #   value: "false"
  # - name: EGOV_ENC_SIGN_HOST
  #   valueFrom:
  #     configMapKeyRef:
  #       name: "egov-service-host"
  #       key: "egov-enc-service"

