apiVersion: v1
kind: Service
metadata:
  name: egf-masters
  namespace: egov
  labels:
    app: egf-masters
    group: financials
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: egf-masters
    group: financials
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: egf-masters
  namespace: egov
spec:
  replicas: {{conf['egf-masters']['replicas']}}
  template:
    metadata:
      labels:
        app: egf-masters
        group: financials
      annotations:
        pod.beta.kubernetes.io/init-containers: '[
          {
            "name": "egf-masters-db-migration",
            "image": "{{conf["egf-masters"]["db_migration_image"]}}",
            "env": [
              {"name": "DB_URL", "valueFrom": {"configMapKeyRef": {"name": "egov-config", "key": "new-db-url"}}},
              {"name": "SCHEMA_TABLE", "value": "egf_masters_schema"},
              {"name": "FLYWAY_USER", "valueFrom": {"secretKeyRef": {"name": "db", "key": "flyway-username"}}},
              {"name": "FLYWAY_PASSWORD", "valueFrom": {"secretKeyRef": {"name": "db", "key": "flyway-password"}}},
              {"name": "FLYWAY_LOCATIONS", "valueFrom": {"configMapKeyRef": {"name": "egov-config", "key": "flyway-locations"}}}
            ],
            "imagePullPolicy": "Always"
          }
        ]'
    spec:
      containers:
      - name: egf-masters
        image: {{conf['egf-masters']['image']}}
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_DATASOURCE_URL
          valueFrom:
            configMapKeyRef:
              name: egov-config
              key: new-db-url
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: db
              key: username
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db
              key: password
        - name: SPRING_DATASOURCE_TOMCAT_INITIAL_SIZE
          valueFrom:
            configMapKeyRef:
              name: egov-config
              key: spring-datasource-tomcat-initialSize
        - name: SERVER_PORT
          value: "8080"
        - name: FLYWAY_ENABLED
          value: "false"