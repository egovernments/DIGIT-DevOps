apiVersion: v1
kind: Service
metadata:
  name: lams-web
  namespace: egov
  labels:
    app: lams-web
    group: lams
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: lams-web
    group: lams
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: lams-web
  namespace: egov
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: lams-web
        group: lams
    spec:
      containers:
      - name: lams-web
        image: egovio/lams-web
        ports:
        - containerPort: 80
      - name: dnsmasq
        image: "janeczku/go-dnsmasq:release-1.0.5"
        args:
          - --listen
          - "127.0.0.1:53"
          - --default-resolver
          - --append-search-domains
          - --hostsfile=/etc/hosts
          - --verbose