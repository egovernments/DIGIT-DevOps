apiVersion: v1
kind: Service
metadata:
  name: pgr-rest
  namespace: egov
  labels:
    app: pgr-rest
    group: pgr
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: pgr-rest
    group: pgr
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: pgr-rest
  namespace: egov
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: pgr-rest
        group: pgr
    spec:
      containers:
      - name: pgr-rest
        image: egovio/pgr-rest
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_DATASOURCE_URL
          valueFrom:
            configMapKeyRef:
              name: egov-config
              key: db-url
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: db
              key: username
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db
              key: password
        - name: FLYWAY_ENABLED
          value: "false"
        - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
          valueFrom:
            configMapKeyRef:
              name: egov-config
              key: kafka-brokers
        - name: SERVER_PORT
          value: "8080"
        - name: CRN_SERVICE_URL
          value: http://pgr-crn-generation:8080/crn-generation/crn
        - name: USER_SERVICE_URL
          value: http://egov-user:8080/user/details?access_token=
        - name: EGOV_SERVICES_EIS_HOST
          value: http://egov-eis:8080/
        - name: OUTGOING_QUEUE_NAME_SUFFIX
          value: egov.pgr.validated