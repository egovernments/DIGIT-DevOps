apiVersion: v1
kind: Service
metadata:
  name: egov-tradelicence
  namespace: egov
  labels:
    app: egov-tradelicence
    group: web
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: egov-tradelicence
    group: web
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: egov-tradelicence
  namespace: egov
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  replicas: {{conf['egov-tradelicence']['replicas']}}
  template:
    metadata:
      labels:
        app: egov-tradelicence
        group: web
    spec:
      containers:
      - name: egov-tradelicence
        image: {{conf['egov-tradelicence']['images'][0]}}
        ports:
        - containerPort: 80
        # readinessProbe:
        #   httpGet:
        #     path: /egov-tradelicence/
        #     port: 80
        #   initialDelaySeconds: 5
        #   periodSeconds: 5
        #   successThreshold: 1
        resources:
          requests:
            memory: "64Mi"
          limits:
            memory: "{{ conf['egov-tradelicence']['memory_limits'] or "64Mi"}}"
