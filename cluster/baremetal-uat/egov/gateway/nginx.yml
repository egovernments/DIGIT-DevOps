apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: egov
  labels:
    app: nginx
    group: gateway
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: nginx
    group: gateway
  type: "LoadBalancer"
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: nginx
  namespace: egov
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: nginx
        group: gateway
    spec:
      containers:
      - name: nginx
        image: egovio/nginx-baremetal-uat:0.0.1
        ports:
        - containerPort: 80
      - name: dnsmasq
        image: "janeczku/go-dnsmasq:release-1.0.5"
        args:
          - --listen
          - "127.0.0.1:53"
          - --default-resolver
          - --append-search-domains
          - --hostsfile=/etc/hosts
          - --verbose