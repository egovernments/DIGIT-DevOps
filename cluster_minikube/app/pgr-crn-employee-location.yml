  apiVersion: v1
  kind: PersistentVolume
  metadata:
    name: pgr-crn-employee-location
  spec:
    capacity:
      storage: 1Gi
    accessModes:
      - ReadWriteOnce
    persistentVolumeReclaimPolicy: Recycle
    claimRef:
      name: pgr-crn-employee-location
      namespace: default
    hostPath:
      path: /var/log
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pgr-crn-employee-location
  annotations:
    volume.beta.kubernetes.io/storage-class: "anything"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: pgr-crn-employee-location
  labels:
    app: pgr-crn-employee-location
spec:
  ports:
  - port: 9001
    targetPort: 9001
    protocol: TCP
  selector:
    app: pgr-crn-employee-location
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: pgr-crn-employee-location
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: pgr-crn-employee-location
    spec:
      volumes:
      - name: logs
        persistentVolumeClaim:
          claimName: pgr-crn-employee-location
      containers:
      - name: pgr-crn-employee-location
        image: nsready/pgr-crn-employee-location
        volumeMounts:
        - mountPath: "/var/log"
          name: logs
        ports:
        - containerPort: 9001
        env:
        - name: SPRING_DATASOURCE_URL
          value: jdbc:postgresql://postgres:5432/mydb
        - name: SPRING_DATASOURCE_USERNAME
          value: postgres
        - name: SPRING_DATASOURCE_PASSWORD
          value: postgres
        - name: KAFKA_CONFIG_BOOTSTRAP_SERVER_CONFIG
          value: kafka:9092
        - name: KAFKA_CONFIG_BOOTSTRAP_SERVER_CONFIG
          value: kafka:9092
        - name: EGOV_SERVICES_BOUNDARY_HOST
          value: http://core-location-user-workflow:9001/
        - name: EGOV_SERVICES_CROSSHIERARCHY_SERVICE_HOST
          value: http://core-location-user-workflow:9001/
        - name: EGOV_SERVICES_WORKFLOW_HOSTNAME
          value: http://core-location-user-workflow:9003/
        - name: KAFKA_TOPICS_PGR_LOCATIONPOPULATED_NAME
          value: egov.pgr.locationupdated
        - name: KAFKA_TOPICS_PGR_LOCATIONPOPULATED_ID
          value: egov.pgr.locationupdated
        - name: KAFKA_TOPICS_PGR_LOCATIONPOPULATED_GROUP
          value: egov.pgr.locationupdated
        - name: KAFKA_TOPICS_PGR_EMPLOYEE_ENRICHED_NAME
          value: egov.pgr.workflowupdated
        - name: KAFKA_TOPICS_PGR_VALIDATED_NAME
          value: egov.pgr.validated
        - name: KAFKA_TOPICS_PGR_VALIDATED_ID
          value: egov.pgr.validated
        - name: KAFKA_TOPICS_PGR_VALIDATED_GROUP
          value: egov.pgr.validated
        - name: KAFKA_TOPICS_PGR_BOUNDARY_ENRICHED_NAME
          value: egov.pgr.locationupdated