apiVersion: v1
kind: Service
metadata:
  name: crn-generation
  namespace: pgr
  labels:
    app: crn-generation
    role: master
    tier: frontend
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: crn-generation
    role: master
    tier: frontend
