apiVersion: v1
kind: Service
metadata:
  name: employee-enrichment
  namespace: pgr
  labels:
    app: employee-enrichment
    role: master
    tier: frontend
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: employee-enrichment
    role: master
    tier: frontend