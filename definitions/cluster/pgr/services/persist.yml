apiVersion: v1
kind: Service
metadata:
  name: persist
  namespace: pgr
  labels:
    app: persist
    role: master
    tier: frontend
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: persist
    role: master
    tier: frontend