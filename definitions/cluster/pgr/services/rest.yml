apiVersion: v1
kind: Service
metadata:
  name: rest
  namespace: pgr
  labels:
    app: rest
    role: master
    tier: frontend
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: rest
    role: master
    tier: frontend
  type: "LoadBalancer"