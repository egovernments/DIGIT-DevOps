apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: web
  labels:
    app: nginx
    role: master
    tier: frontend
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: nginx
    role: master
    tier: frontend
  type: "LoadBalancer"