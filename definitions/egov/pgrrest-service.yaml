apiVersion: v1
kind: Service
metadata:
  name: egov-pgrrest
  labels:
    app: egov-pgrrest
    tier: backend
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: egov-pgrrest
    tier: backend
