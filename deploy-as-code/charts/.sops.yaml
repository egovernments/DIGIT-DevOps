creation_rules:
  # upon creation of a file that matches the pattern *dev.yaml,
  # KMS set A is used
  # eGOV Internal ------------------------------------------------------------------------------------------------------------- #
  - path_regex: environments/env\-secrets\.yaml$
    gcp_kms: 'projects/mcs-gcp-test-1/locations/asia-south1/keyRings/digit-keyring/cryptoKeys/digit-key'