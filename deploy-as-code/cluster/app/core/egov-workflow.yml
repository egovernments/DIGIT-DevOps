apiVersion: v1
kind: Service
metadata:
  name: egov-workflow
  namespace: core
  labels:
    app: egov-workflow
    role: master
    tier: frontend
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: egov-workflow
    role: master
    tier: frontend
  type: "LoadBalancer"
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: egov-workflow
  namespace: core
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: egov-workflow
        role: master
        tier: frontend
    spec:
      containers:
      - name: egov-workflow
        image: egovio/egov-workflow
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_DATASOURCE_URL
          valueFrom:
            configMapKeyRef:
              name: db
              key: url
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: db
              key: username
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db
              key: password
        - name: EGOV_SERVICES_BOUNDARY_HOST
          value: http://egov-location.core:8080/
        - name: EGOV_SERVICES_PGRREST_HOST
          value: http://pgr-rest.pgr:8080/
        - name: EGOV_SERVICES_EIS_HOST
          value: http://egov-eis.core:8080/
        - name: SERVER_PORT
          value: "8080"