apiVersion: v1
kind: Service
metadata:
  name: egov-hr-masters
  namespace: egov
  labels:
    app: egov-hr-masters
    group: hr
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: egov-hr-masters
    group: hr
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: egov-hr-masters
  namespace: egov
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: egov-hr-masters
        group: hr
    spec:
      containers:
      - name: egov-hr-masters
        image: egovio/egov-hr-masters
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_DATASOURCE_URL
          valueFrom:
            configMapKeyRef:
              name: egov-config
              key: db-url
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: db
              key: username
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db
              key: password
        - name: SERVER_PORT
          value: "8080"
        - name: FLYWAY_ENABLED
          value: "false"