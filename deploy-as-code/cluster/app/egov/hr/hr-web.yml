apiVersion: v1
kind: Service
metadata:
  name: hr-web
  namespace: egov
  labels:
    app: hr-web
    group: hr
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: hr-web
    group: hr
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: hr-web
  namespace: egov
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: hr-web
        group: hr
    spec:
      containers:
      - name: hr-web
        image: egovio/hr-web
        ports:
        - containerPort: 80
      - name: dnsmasq
        image: "janeczku/go-dnsmasq:release-1.0.5"
        args:
          - --listen
          - "127.0.0.1:53"
          - --default-resolver
          - --append-search-domains
          - --hostsfile=/etc/hosts
          - --verbose