apiVersion: v1
kind: Service
metadata:
  name: pt-user-enrichment
  namespace: egov
  labels:
    app: pt-user-enrichment
    group: property
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: pt-user-enrichment
    group: property
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: pt-user-enrichment
  namespace: egov
spec:
  replicas: {{conf['pt-user-enrichment']['replicas']}}
  template:
    metadata:
      labels:
        app: pt-user-enrichment
        group: property
      annotations:
        pod.beta.kubernetes.io/init-containers: '[
          {
            "name": "pt-user-enrichment-db-migration",
            "image": "{{conf["pt-user-enrichment"]["db_migration_image"]}}",
            "env": [
              {"name": "DB_URL", "valueFrom": {"configMapKeyRef": {"name": "egov-config", "key": "db-url"}}},
              {"name": "SCHEMA_TABLE", "value": "pt_user_enrichment_schema"},
              {"name": "FLYWAY_USER", "valueFrom": {"secretKeyRef": {"name": "db", "key": "flyway-username"}}},
              {"name": "FLYWAY_PASSWORD", "valueFrom": {"secretKeyRef": {"name": "db", "key": "flyway-password"}}},
              {"name": "FLYWAY_LOCATIONS", "valueFrom": {"configMapKeyRef": {"name": "egov-config", "key": "flyway-locations"}}}
            ],
            "imagePullPolicy": "IfNotPresent"
          }
        ]'
    spec:
      containers:
      - name: pt-user-enrichment
        image: {{conf['pt-user-enrichment']['images'][0]}}
        ports:
        - containerPort: 8080
        env:
        - name: UPDATE_USER
          value: createProperty
        - name: VALIDATE_USER
          value: createUserValidate
        - name: VALIDATE_UPDATE_USER
          value: updateUserValidate
        - name: UPDATE_PROPERTY_USER
          value: updatePropery
        - name: BOOTSTRAP_SERVERS  ## perhaps SPRING_KAFKA_BOOTSTRAP_SERVERS ???
          valueFrom:
            configMapKeyRef:
              name: egov-config
              key: kafka-brokers
        - name: AUTO_OFFSET_RESET_CONFIG
          value: earliest
        - name: EGOV_SERVICES_ALLOTTEE_SERVICE_HOSTNAME
          value: http://egov-user:8080/
        - name: SERVER_CONTEXTPATH
          value: /pt-user-enrichment
        - name: APP_TIMEZONE
          valueFrom:
            configMapKeyRef:
              name: egov-config
              key: timezone
