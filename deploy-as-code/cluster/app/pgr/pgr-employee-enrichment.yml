apiVersion: v1
kind: Service
metadata:
  name: pgr-employee-enrichment
  namespace: pgr
  labels:
    app: pgr-employee-enrichment
    role: master
    tier: frontend
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: pgr-employee-enrichment
    role: master
    tier: frontend
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: pgr-employee-enrichment
  namespace: pgr
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: pgr-employee-enrichment
        role: master
        tier: worker
    spec:
      containers:
      - name: pgr-employee-enrichment
        image: egovio/pgr-employee-enrichment
        ports:
        - containerPort: 8080
        env:
        - name: EGOV_SERVICES_CROSSHIERARCHY_SERVICE_HOST
          value: http://location.core:8080/
        - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
          value: kafka.backbone:9092
        - name: EGOV_SERVICES_WORKFLOW_HOSTNAME
          value: http://egov-workflow.core:8080/
        - name: KAFKA_TOPICS_PGR_LOCATIONPOPULATED_NAME
          value: egov.pgr.locationupdated
        - name: KAFKA_TOPICS_PGR_LOCATIONPOPULATED_ID
          value: egov.pgr.locationupdated
        - name: KAFKA_TOPICS_PGR_LOCATIONPOPULATED_GROUP
          value: egov.pgr.locationupdated
        - name: KAFKA_TOPICS_PGR_EMPLOYEE_ENRICHED_NAME
          value: egov.pgr.workflowupdated