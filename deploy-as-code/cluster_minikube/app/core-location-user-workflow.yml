  apiVersion: v1
  kind: PersistentVolume
  metadata:
    name: core-location-user-workflow
  spec:
    capacity:
      storage: 1Gi
    accessModes:
      - ReadWriteOnce
    persistentVolumeReclaimPolicy: Recycle
    claimRef:
      name: core-location-user-workflow
      namespace: default
    hostPath:
      path: /var/log
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: core-location-user-workflow
  annotations:
    volume.beta.kubernetes.io/storage-class: "anything"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: core-location-user-workflow
  labels:
    app: core-location-user-workflow
spec:
  ports:
  - port: 9001
    targetPort: 9001
    protocol: TCP
    name: location
  - port: 9002
    targetPort: 9002
    protocol: TCP
    name: user
  - port: 9003
    targetPort: 9003
    protocol: TCP
    name: workflow
  selector:
    app: core-location-user-workflow
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: core-location-user-workflow
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: core-location-user-workflow
    spec:
      volumes:
      - name: logs
        persistentVolumeClaim:
          claimName: core-location-user-workflow
      containers:
      - name: core-location-user-workflow
        image: nsready/core-location-user-workflow
        volumeMounts:
        - mountPath: "/var/log"
          name: logs
        ports:
        - containerPort: 9001
          name: location
        - containerPort: 9002
          name: user
        - containerPort: 9003
          name: workflow
        env:
        - name: SPRING_DATASOURCE_URL
          value: jdbc:postgresql://postgres:5432/mydb
        - name: SPRING_DATASOURCE_USERNAME
          value: postgres
        - name: SPRING_DATASOURCE_PASSWORD
          value: postgres
        - name: SPRING_REDIS_HOST
          value: redis
        - name: SPRING_REDIS_PORT
          value: "6379"
        - name: EGOV_SERVICES_BOUNDARY_HOST
          value: http://localhost:9001/
        - name: EGOV_SERVICES_PGRREST_HOST
          value: http://pgr-rest-persist-search:9001/
        - name: EGOV_SERVICES_EIS_HOST
          value: http://core-eis-filestore-localization:9001/
        - name: EGOV_SERVICES_USER_HOST
          value: http://localhost:9002/

