apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: backbone
  labels:
    app: postgres
    role: master
    tier: backend
spec:
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app: postgres
    role: master
    tier: backend