apiVersion: v1
kind: Service
metadata:
  name: zookeeper
  namespace: backbone
  labels:
    app: zookeeper
    role: master
    tier: backend
spec:
  ports:
  - port: 2181
    targetPort: 2181
  selector:
    app: zookeeper
    role: master
    tier: backend