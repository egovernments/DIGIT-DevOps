apiVersion: v1
kind: Service
metadata:
  name: eis
  namespace: common
  labels:
    app: eis
    role: master
    tier: frontend
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: eis
    role: master
    tier: frontend