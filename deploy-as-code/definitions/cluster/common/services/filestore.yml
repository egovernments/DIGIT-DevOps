apiVersion: v1
kind: Service
metadata:
  name: filestore
  namespace: common
  labels:
    app: filestore
    role: master
    tier: frontend
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: filestore
    role: master
    tier: frontend
  type: "LoadBalancer"