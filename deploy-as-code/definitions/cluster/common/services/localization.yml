apiVersion: v1
kind: Service
metadata:
  name: localization
  namespace: common
  labels:
    app: localization
    role: master
    tier: frontend
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: localization
    role: master
    tier: frontend
  type: "LoadBalancer"