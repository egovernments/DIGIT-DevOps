apiVersion: v1
kind: Service
metadata:
  name: location
  namespace: common
  labels:
    app: location
    role: master
    tier: frontend
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: location
    role: master
    tier: frontend