apiVersion: v1
kind: Service
metadata:
  name: workflow
  namespace: common
  labels:
    app: workflow
    role: master
    tier: frontend
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: workflow
    role: master
    tier: frontend