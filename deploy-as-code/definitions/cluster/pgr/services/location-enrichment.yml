apiVersion: v1
kind: Service
metadata:
  name: location-enrichment
  namespace: pgr
  labels:
    app: location-enrichment
    role: master
    tier: frontend
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: location-enrichment
    role: master
    tier: frontend