# creation rules are evaluated sequentially, the first match wins
creation_rules:
        # upon creation of a file that matches the pattern *dev.yaml,
        # KMS set A is used
        # eGOV Internal ------------------------------------------------------------------------------------------------------------- #
        - path_regex: environments/aws\-go\-secrets\.yaml$
          kms: 'arn:aws:kms:ap-south-1:349271159511:key/7f14b0e9-2f3b-49b1-82ea-78f3874f8869'
        - path_regex: environments/gcp\-go\-secrets\.yaml$
          gcp_kms: 'projects/digit-platform/locations/asia-south1/keyRings/digit-platform-sops-keyring/cryptoKeys/digit-platform-sops-key'
