apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: care-project
  namespace: argocd
spec:
  description: care services for the Azure AKS unified-dev environment
  sourceRepos:
    - https://github.com/egovernments/DIGIT-DevOps.git

  destinations:
    - namespace: 'care'
      server: https://kubernetes.default.svc
