apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: unified-qa-egov-appset-core
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - name: audit-service
          - name: boundary-service
          - name: egov-accesscontrol
          - name: egov-enc-service
          - name: egov-filestore
          - name: egov-idgen
          - name: egov-indexer
          - name: egov-localization
          - name: egov-location
          - name: egov-mdms-service
          - name: egov-notification-mail
          - name: egov-notification-sms
          - name: egov-otp
          - name: egov-persister
          - name: egov-pg-service
          - name: egov-url-shortening
          - name: egov-user-event
          - name: egov-workflow-v2
          - name: filestore
          - name: gateway
          - name: inbox
          - name: mdms-v2
          - name: pdf-service
          - name: user-otp

  template:
    metadata:
      name: 'egov-{{name}}'

    spec:
      project: egov-project

      source:
        repoURL: https://github.com/egovernments/DIGIT-DevOps.git
        targetRevision: unified-env-lts
        path: deploy-as-code/helm/charts/core-services/{{name}}
        helm:
          valueFiles:
            - values.yaml
            - ../../../environments/unified-qa.yaml
          # keep rendered manifests in the destination namespace regardless of the
          # chart default (e.g. xstate-chatbot ships namespace: urban)
          parameters:
            - name: namespace
              value: egov

      destination:
        server: https://kubernetes.default.svc
        namespace: 'egov'

      syncPolicy:
        automated:
          prune: false
          selfHeal: true
        syncOptions:
          - ApplyOutOfSyncOnly=true
          - ServerSideApply=true
        retry:
          limit: 10
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m0s

      ignoreDifferences:
        - group: apps
          kind: Deployment
          jsonPointers:
            - /spec/replicas
