apiVersion: v2
name: binfmt
description: Registers QEMU binfmt_misc handlers on every node, enabling multi-arch container builds (linux/arm64) on AMD64 nodes via QEMU user-space emulation.
type: application
version: 0.1.0
appVersion: "qemu-v8.1.5"
