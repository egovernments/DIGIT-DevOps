apiVersion: v2
name: debezium-postgres-connect
description: Debezium Kafka Connect worker for PostgreSQL CDC, publishing to the existing Kafka KRaft cluster
type: application
version: 0.1.0
appVersion: 3.0.0.Final

dependencies:
- name: common
  version: 0.0.5
  repository: file://../../common
