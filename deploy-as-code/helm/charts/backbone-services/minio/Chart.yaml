apiVersion: v2
name: minio
description: >-
  MinIO distributed object storage (S3-compatible), reconstructed from the
  in-cluster deployment in the AWS EKS digit-lts cluster (namespace: default).
keywords:
  - minio
  - storage
  - object-storage
  - s3
  - distributed
version: 1.0.0
# appVersion tracks the running MinIO server image release.
appVersion: "RELEASE.2024-12-18T13-15-44Z"
sources:
  - https://github.com/minio/minio
