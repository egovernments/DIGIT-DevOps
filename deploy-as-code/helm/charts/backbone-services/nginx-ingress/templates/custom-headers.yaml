{{- $serviceName := include "nginx-ingress.name" . -}}
apiVersion: v1
data:
{{- tpl (index .Values "controller" "custom-headers") . | nindent 2 }}
kind: ConfigMap
metadata:
  name: custom-headers
  namespace: {{ .Values.namespace }}