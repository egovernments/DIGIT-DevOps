# Common Labels
labels:
  app: "redoc"
  group: "docs"

# Ingress Configs
ingress:
  enabled: true
  context: "redoc"

# Init Containers Configs
initContainers: {}

# Container Configs
image:
  repository: "redoc"
replicas: "1"
httpPort: 80
