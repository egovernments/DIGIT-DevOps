namespace: egov
name: s3-proxy

cluster-configs:
  configmaps:
    egov-config:
      data:
        s3-assets-bucket: "(unified-demo-central-instance)"

externalName: s3.ap-south-1.amazonaws.com
httpPort: 80
type: ExternalName