apiVersion: v2
name: temporal
description: DIGIT-style Helm chart for Temporal (temporalio/temporal) — split-service server, web UI, Postgres (default store) + Elasticsearch (visibility)
type: application

# Chart version. Bump on every change to the chart/templates.
version: 0.1.0

# Temporal server / admin-tools version this chart targets.
appVersion: "1.31.0"
