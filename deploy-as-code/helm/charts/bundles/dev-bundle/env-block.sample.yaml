# Sample environment override block for the dev-bundle chart —
# paste into environments/<env>.yaml and fill in the image tags.
dev-bundle:
  image:
    repository: dev-bundle
    tag: "<bundle-image-tag>"
  # Any single env var can be overridden (maps deep-merge):
  # env:
  #   DB_HOST: {value: "postgres.egov"}
  #   TENANT_MIGRATION_ENABLED: {value: "true"}
  #   FILESTORE_S3_SOURCE: {value: "s3"}    # flip a dev pin from the bundle jar
  dbMigrations:
    idgen:
      image: {repository: idgen-db, tag: "<tag>"}
    template-config:
      image: {repository: template-config-db, tag: "<tag>"}
    billing:
      image: {repository: billing-db, tag: "<tag>"}
    apportion:
      image: {repository: apportion-db, tag: "<tag>"}
    url-shortener:
      image: {repository: url-shortener-db, tag: "<tag>"}
    pg-service:
      image: {repository: pg-service-db, tag: "<tag>"}
    otp:
      image: {repository: otp-db, tag: "<tag>"}
    notification:
      image: {repository: notification-db, tag: "<tag>"}
    employee:
      image: {repository: employee-db, tag: "<tag>"}
    individual:
      image: {repository: individual-db, tag: "<tag>"}
    workflow:
      image: {repository: workflow-db, tag: "<tag>"}
    registry:
      image: {repository: registry-db, tag: "<tag>"}
    filestore:
      image: {repository: filestore-db, tag: "<tag>"}
    localization:
      image: {repository: localization-db, tag: "<tag>"}
    account:
      image: {repository: account-db, tag: "<tag>"}
    boundary:
      image: {repository: boundary-db, tag: "<tag>"}
