{{- if index .Values "db" "db-ips" }}
kind: Endpoints
apiVersion: v1
metadata:
 name: "{{ .Values.db.name }}-endpoints"
subsets:
 - addresses:
    {{- toYaml index .Values "db" "db-ips" | nindent 4 }}
   ports:
     - port: {{ .Values.db.targetPort }}
{{- end }}
