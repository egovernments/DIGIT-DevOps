{{- if index .Values "db" "db-ips" }}
kind: Service
apiVersion: v1
metadata:
 name: {{ .Values.db.name }}
 namespace: {{ .Values.db.namespace }}
Spec:
 type: ClusterIP
 ports:
 - port: {{ .Values.db.port }}
   targetPort: {{ .Values.db.targetPort }}
{{- end }}
