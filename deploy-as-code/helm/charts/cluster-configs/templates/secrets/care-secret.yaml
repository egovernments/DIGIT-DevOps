{{- with index .Values "cluster-configs" "secrets" "care" }}
{{- if index . "DATABASE_URL" }}
---
{{- range $ns := .namespace }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ index $.Values "cluster-configs" "secrets" "care" "name" | default "care-secrets" }}
  namespace: {{ $ns }}
  labels:
    app: care
type: Opaque
stringData:
{{- range $key, $value := index $.Values "cluster-configs" "secrets" "care" }}
{{- if and (ne $key "namespace") (ne $key "name") }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
{{- end }}
---
{{- end }}
{{- end }}
{{- end }}
