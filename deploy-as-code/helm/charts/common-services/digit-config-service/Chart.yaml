apiVersion: v2
name: digit-config-service
description: Centralized configuration management service for runtime configs, templates, feature flags, and notification policies
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
- name: common
  version: 0.0.5
  repository: file://../../common
