# Common Labels
labels:
  app: "estimate-service"
  group: "digit-works"

namespace: works

# Ingress Configs
ingress:
  namespace: egov
  enabled: true
  zuul: true
  context: "estimate-service"

# Init Containers Configs
initContainers:
  dbMigration:
    enabled: true
    schemaTable: "estimate_service_schema"
    image:
      repository: "estimate-service-db"

# Container Configs
image:
  repository: "estimate-service"
replicas: "1"
healthChecks:
  enabled: true
  livenessProbePath: "/estimate-service/health"
  readinessProbePath: "/estimate-service/health"
appType: "java-spring"
tracing-enabled: true
heap: "-Xmx192m -Xms192m"
java-args: ""

# Additional Container Envs
env: |
  - name: SERVER_PORT
    value: "8080"
  - name: JAVA_OPTS
    value: {{ index .Values "heap" | quote }}
  - name: JAVA_ARGS
    value: {{ index .Values "java-args" | quote }}
  {{- if index .Values "tracing-enabled" }}
  - name: TRACER_OPENTRACING_ENABLED
    value: "true" 
  {{- end }}
  - name: SERVER_CONTEXT_PATH
    value: /estimate-service
  - name: SERVER_SERVLET_CONTEXT-PATH
    value: /estimate-service  
  - name: EGOV_MDMS_HOST
    valueFrom:
      configMapKeyRef:
        name: egov-service-host
        key: egov-mdms-service
