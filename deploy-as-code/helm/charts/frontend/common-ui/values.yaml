# Common Labels
labels:
  app: "common-ui"
  group: "web"

namespace: egov    

# Ingress Configs
ingress:
  enabled: true
  context: "common-ui"

# Init Containers Configs
initContainers: {}

# Container Configs
image:
  repository: "common-ui"
replicas: "1"
httpPort: 80
healthChecks:
  enabled: false
  livenessProbePath: "/common-ui/"
  readinessProbePath: "/common-ui/"

extraVolumes: |
  - name: js-injection
    configMap:
      name: common-ui-js-injection

extraVolumeMounts: |
  - mountPath: /etc/nginx/conf.d/sub_filter.conf
    name: js-injection
    subPath: sub_filter.conf
