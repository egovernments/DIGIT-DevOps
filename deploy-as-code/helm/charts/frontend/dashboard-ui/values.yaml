# Common Labels
labels:
  app: "dashboard-ui"
  group: "web"

namespace: health    

# Ingress Configs
ingress:
  enabled: true
  context: "dashboard-ui"

# Init Containers Configs
initContainers: {}

# Container Configs
image:
  repository: "dashboard-ui"
replicas: "1"
appType: "java-spring"
httpPort: 80
healthChecks:
  enabled: true
  livenessProbePath: "/dashboard-ui/"
  readinessProbePath: "/dashboard-ui/"
tracing-enabled: true
memory_limits: "256Mi"
cpu_limits: '100m'

extraVolumes: |
  - name: js-injection
    configMap:
      name: dashboard-ui-js-injection

extraVolumeMounts: |
  - mountPath: /etc/nginx/conf.d/sub_filter.conf
    name: js-injection
    subPath: sub_filter.conf
