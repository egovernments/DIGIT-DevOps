# Common Labels
labels:
  app: "dss-ui"
  group: "web"

namespace: egov    

# Ingress Configs
ingress:
  enabled: true
  context: "dss-ui"

# Init Containers Configs
initContainers: {}

# Container Configs
image:
  repository: "dss-ui"
replicas: "1"
httpPort: 80
healthChecks:
  enabled: false
  livenessProbePath: "/dss-ui/"
  readinessProbePath: "/dss-ui/"

extraVolumes: |
  - name: js-injection
    configMap:
      name: dss-ui-js-injection

extraVolumeMounts: |
  - mountPath: /etc/nginx/conf.d/sub_filter.conf
    name: js-injection
    subPath: sub_filter.conf
