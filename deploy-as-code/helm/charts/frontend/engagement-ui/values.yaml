# Common Labels
labels:
  app: "engagement-ui"
  group: "web"

namespace: egov    

# Ingress Configs
ingress:
  enabled: true
  context: "engagement-ui"

# Init Containers Configs
initContainers: {}

# Container Configs
image:
  repository: "engagement-ui"
replicas: "1"
httpPort: 80
healthChecks:
  enabled: true
  livenessProbePath: "/engagement-ui/"
  readinessProbePath: "/engagement-ui/"

extraVolumes: |
  - name: js-injection
    configMap:
      name: engagement-ui-js-injection

extraVolumeMounts: |
  - mountPath: /etc/nginx/conf.d/sub_filter.conf
    name: js-injection
    subPath: sub_filter.conf
