# Common Labels
labels:
  app: "fsm-ui"
  group: "web"

namespace: egov    

# Ingress Configs
ingress:
  enabled: true
  context: "fsm-ui"

# Init Containers Configs
initContainers: {}

# Container Configs
image:
  repository: "fsm-ui"
replicas: "1"
httpPort: 80
healthChecks:
  enabled: true
  livenessProbePath: "/fsm-ui/"
  readinessProbePath: "/fsm-ui/"

extraVolumes: |
  - name: js-injection
    configMap:
      name: fsm-ui-js-injection

extraVolumeMounts: |
  - mountPath: /etc/nginx/conf.d/sub_filter.conf
    name: js-injection
    subPath: sub_filter.conf
