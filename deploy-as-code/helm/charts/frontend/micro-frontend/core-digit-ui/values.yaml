# Common Labels
labels:
  app: "core-digit-ui"
  group: "web"

namespace: egov    

# Ingress Configs
ingress:
  enabled: true
  context: "core-digit-ui"

# Init Containers Configs
initContainers: {}

# Container Configs
image:
  repository: "core-digit-ui"
replicas: "1"
httpPort: 80
healthChecks:
  enabled: true
  livenessProbePath: "/core-digit-ui/"
  readinessProbePath: "/core-digit-ui/"

extraVolumes: |
  - name: js-injection
    configMap:
      name: core-digit-ui-js-injection

extraVolumeMounts: |
  - mountPath: /etc/nginx/conf.d/sub_filter.conf
    name: js-injection
    subPath: sub_filter.conf
