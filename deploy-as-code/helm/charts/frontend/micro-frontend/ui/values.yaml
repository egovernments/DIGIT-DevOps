# Common Labels
labels:
  app: "ui"
  group: "web"

namespace: egov    

# Ingress Configs
ingress:
  enabled: true
  context: "ui"

# Init Containers Configs
initContainers: {}

# Container Configs
image:
  repository: "ui"
replicas: "1"
httpPort: 80
healthChecks:
  enabled: true
  livenessProbePath: "/ui/"
  readinessProbePath: "/ui/"

extraVolumes: |
  - name: js-injection
    configMap:
      name: ui-js-injection

extraVolumeMounts: |
  - mountPath: /etc/nginx/conf.d/sub_filter.conf
    name: js-injection
    subPath: sub_filter.conf
