# Common Labels
labels:
  app: "pgr-ui"
  group: "web"

namespace: egov    

# Ingress Configs
ingress:
  enabled: true
  context: "pgr-ui"

# Init Containers Configs
initContainers: {}

# Container Configs
image:
  repository: "pgr-ui"
replicas: "1"
httpPort: 80
healthChecks:
  enabled: true
  livenessProbePath: "/pgr-ui/"
  readinessProbePath: "/pgr-ui/"

extraVolumes: |
  - name: js-injection
    configMap:
      name: pgr-ui-js-injection

extraVolumeMounts: |
  - mountPath: /etc/nginx/conf.d/sub_filter.conf
    name: js-injection
    subPath: sub_filter.conf
