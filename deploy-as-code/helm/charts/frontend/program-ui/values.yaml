# Common Labels
labels:
  app: "program-ui"
  group: "web"

namespace: egov    

# Ingress Configs
ingress:
  enabled: true
  context: "program-ui"

# Init Containers Configs
initContainers: {}

# Container Configs
image:
  repository: "program-ui"
replicas: "1"
httpPort: 80
healthChecks:
  enabled: false
  livenessProbePath: "/program-ui/"
  readinessProbePath: "/program-ui/"

extraVolumes: |
  - name: js-injection
    configMap:
      name: program-ui-js-injection

extraVolumeMounts: |
  - mountPath: /etc/nginx/conf.d/sub_filter.conf
    name: js-injection
    subPath: sub_filter.conf
