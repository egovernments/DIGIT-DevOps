# Common Labels
labels:
  app: "sample-ui"
  group: "web"

namespace: egov    

# Ingress Configs
ingress:
  enabled: true
  context: "sample-ui"

# Init Containers Configs
initContainers: {}

# Container Configs
image:
  repository: "sample-ui"
replicas: "1"
httpPort: 80
healthChecks:
  enabled: false
  livenessProbePath: "/sample-ui/"
  readinessProbePath: "/sample-ui/"

extraVolumes: |
  - name: js-injection
    configMap:
      name: sample-ui-js-injection

extraVolumeMounts: |
  - mountPath: /etc/nginx/conf.d/sub_filter.conf
    name: js-injection
    subPath: sub_filter.conf
