# Common Labels
labels:
  app: "sandbox-ui"
  group: "web"

namespace: egov    

# Ingress Configs
ingress:
  enabled: true
  context: "sandbox-ui"

# Init Containers Configs
initContainers: {}

# Container Configs
image:
  repository: "sandbox-ui"
replicas: "1"
httpPort: 80
healthChecks:
  enabled: false
  livenessProbePath: "/sandbox-ui/"
  readinessProbePath: "/sandbox-ui/"

extraVolumes: |
  - name: js-injection
    configMap:
      name: sandbox-ui-js-injection

extraVolumeMounts: |
  - mountPath: /etc/nginx/conf.d/sub_filter.conf
    name: js-injection
    subPath: sub_filter.conf
