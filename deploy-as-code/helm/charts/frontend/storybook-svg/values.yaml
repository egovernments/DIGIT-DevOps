namespace : egov
# Common Labels
labels:
  app: "storybook-svg"
  group: "web"

# Ingress Configs
ingress:
  enabled: true
  context: "storybook-svg"

# Init Containers Configs
initContainers: {}

# Container Configs
image:
  repository: "storybook-svg"
replicas: "1"
httpPort: 80
healthChecks:
  enabled: true
  livenessProbePath: "/storybook-svg/"
  readinessProbePath: "/storybook-svg/"

namespace: egov    
extraVolumes: |
  - name: js-injection
    configMap:
      name: storybook-svg-js-injection

extraVolumeMounts: |
  - mountPath: /etc/nginx/conf.d/sub_filter.conf
    name: js-injection
    subPath: sub_filter.conf
