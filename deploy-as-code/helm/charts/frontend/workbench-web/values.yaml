# Common Labels
labels:
  app: "workbench-web"
  group: "web"

namespace: egov    

# Ingress Configs
ingress:
  enabled: true
  context: "workbench-web"

# Init Containers Configs
initContainers: {}

# Container Configs
image:
  repository: "workbench-web"
replicas: "1"
httpPort: 80
healthChecks:
  enabled: true
  livenessProbePath: "/workbench-web/"
  readinessProbePath: "/workbench-web/"

extraVolumes: |
  - name: js-injection
    configMap:
      name: workbench-web-js-injection

extraVolumeMounts: |
  - mountPath: /etc/nginx/conf.d/sub_filter.conf
    name: js-injection
    subPath: sub_filter.conf
