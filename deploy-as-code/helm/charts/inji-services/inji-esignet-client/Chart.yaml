apiVersion: v2
name: inji-esignet-client
description: >-
  Registers the Mimoto OIDC client with eSignet through its own validated
  client-management API. Idempotent: GETs the client first and POSTs only when
  absent. Never issues PUT or DELETE.
type: application
version: 0.1.0
appVersion: 0.1.0
