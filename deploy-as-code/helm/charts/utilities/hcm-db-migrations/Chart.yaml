apiVersion: v2
name: hcm-db-migrations
description: Alembic database migrations for HCM Reports

type: application
version: 0.1.0
appVersion: "1.0.0"

# Note: This chart uses custom Job template, not common library
# because migrations run as one-time jobs, not deployments
