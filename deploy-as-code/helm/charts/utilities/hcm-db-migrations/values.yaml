# HCM Database Migrations Helm Chart Values

namespace: health

# Git repository containing migration files
# Override these in environment file (unified-dev.yaml, unified-uat.yaml, etc.)
git:
  repo: ""      # Set in environment file
  branch: ""    # Set in environment file
