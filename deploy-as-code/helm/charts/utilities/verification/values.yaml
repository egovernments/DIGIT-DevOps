# Common Labels
labels:
  app: "verification"
  group: "rainmaker"

# Ingress Configs
ingress:
  enabled: true
  zuul: false
  context: "verification"

# Container Configs
image:
  repository: "verification"
replicas: "1"
