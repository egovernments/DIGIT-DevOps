def env = "${env.ENVIRONMENT}"
def group = "${env.GROUP}"

try {
    node("slave"){
        checkout scm
        def deployer = load("jenkins/deployer.groovy")
	      deployer.takeSnapshot(group, env)
    }
} catch (e) {
    throw e
}
