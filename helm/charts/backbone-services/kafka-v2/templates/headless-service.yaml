apiVersion: v1
kind: Service
metadata:
  name: {{ template "name" . }}-headless
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ template "name" . }} 
spec:
  ports:
    - port: 9092
      name: broker
  clusterIP: None
  selector:
    app: {{ template "name" . }}
    