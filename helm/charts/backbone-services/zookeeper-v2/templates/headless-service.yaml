apiVersion: v1
kind: Service
metadata:
  name: "{{ template "name" . }}-headless"
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ template "name" . }}
spec:
  ports:
    - port: 2888
      name: server
    - port: 3888
      name: leader-election
  clusterIP: None
  selector:
    app: {{ template "name" . }}