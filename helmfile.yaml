# helmfile.yaml

# repositories:
#   - name: your-chart-repo
#     url: https://your.chart.repo.url # Replace with the URL of your chart repository, if you have one

releases:
  - name: digit-chatbot-service
    namespace: helmfile # Replace with your desired namespace
    chart: ./charts/core-services/chatbot-service
    version: 0.1.0 # Replace with the version of your chart, if needed

  - name: digit-audit-service
    namespace: helmfile # Replace with your desired namespace
    chart: ./charts/core-services/audit-service
    version: 0.1.0 # Replace with the version of your chart, if needed
