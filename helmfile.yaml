helmfiles:
  - path: ./charts/core-services/coreservices-helmfile.yaml
  - path: ./charts/backbone-services/backbone-helmfile.yaml

environments:
  dev:
    values:
    - environments/dev/values.yaml
  qa:
    values:
    - environments/qa/values.yaml