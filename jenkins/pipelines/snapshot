def group = "${env.GROUP}"
def source_env = "${env.ENVIRONMENT}"

try {
    node("slave"){
        checkout scm
        def deployer = load("jenkins/deployer.groovy")
	      deployer.takeSnapshot(group, source_env)
    }
} catch (e) {
    throw e
}
